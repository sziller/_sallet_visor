kivy  # pip3 install kivy
opencv-python>=4.10.0.84  # Use min. version 4.10.0.84 to fix vulnerabilities - pip3 install opencv-python - cv2 inside
# sudo apt-get install zbar-tools
pyzbar  # pip3 install pyzbar
python-dotenv  # pip3 install python-dotenv
sqlalchemy  # pip3 install sqlalchemy
pyaml  # pip3 install pyaml
pyqrcode  # pip3 install pyqrcode
bitcoinlib      # needed - bitcoin management
requests>=2.32.0  # Use version 2.32.0 or higher to fix vulnerabilities - needed for Node-calls
